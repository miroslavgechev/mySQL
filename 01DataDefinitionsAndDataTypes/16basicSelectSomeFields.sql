SELECT 
    name
FROM
    towns
ORDER BY towns.name;

SELECT 
    name
FROM
    departments
ORDER BY departments.name;

SELECT 
    first_name, last_name, job_title, salary
FROM
    employees
ORDER BY employees.salary DESC;
    